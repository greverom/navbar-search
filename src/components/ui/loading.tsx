import { LoadingContainer, Spinner } from "../../styles/loading.style";

const Loading = () => {
  return (
    <LoadingContainer>
      <Spinner></Spinner>
    </LoadingContainer>
  );
};

export default Loading;